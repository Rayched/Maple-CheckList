/**
 * 'BookmarkList'라는 이름이 들어가는 컴포넌트에서
 * 공통적으로 사용되는 css style code들을 모아둔 파일
 */

import styled from "styled-components";

const BookmarkListContainer = styled.div``;

export const BookmarkList_Commons = {
    BookmarkListContainer,
};